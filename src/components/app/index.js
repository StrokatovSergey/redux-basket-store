import React from 'react';
import ErrorIndicator from '../error-indicator';

const App = () => {
    return (
        <div>
            <ErrorIndicator/>
        </div>
    );
};

export default App;
